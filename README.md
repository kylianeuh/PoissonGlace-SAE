# PoissonGlace-SAE

-- GIT --




Changer de Branche ("-c" c'est pour la créer donc a faire que au début)
git switch -c nom-de-la-branche


Pour récupérer la dernière version
git pull


Pour envoyer tout ce qu'on a ajouté
git add .
git commit -m "ajouter le message ici (IMPORTANT)"
git push

git push origin nom-de-la-branche
et après il faudra voir directement sur github
