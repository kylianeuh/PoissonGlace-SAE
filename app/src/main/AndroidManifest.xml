<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        tools:replace="android:allowBackup,android:fullBackupContent"
        android:icon="@drawable/fish_brown"
        android:label="Poisson Glace V1"
        android:roundIcon="@drawable/fish_brown"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".GestionJoueurActivity" />
        <activity android:name=".ReglesActivity" android:exported="false" />
        <activity android:name=".StatsActivity" android:exported="false" />
        <activity android:name=".Jeu" android:exported="false" />

        <service android:name=".MusiqueService" android:enabled="true" android:exported="false" />
    </application>

</manifest>